# Minimal backend deps (no C compiler needed). Use this if pip install -r requirements.txt fails (numpy/pandas build).
# Upload loan docs + dashboard API work with this. For CSV/Excel processing, install full requirements later.
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
